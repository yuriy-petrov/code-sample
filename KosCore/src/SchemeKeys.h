#ifndef SCHEMEKEYS_H
#define SCHEMEKEYS_H

#include <QColor>

namespace KosConsts
{
    static const QColor OPENED_VALVE_COLOR = QColor(0, 0, 0);
    static const QColor MAGIC_COLOR = QColor(16, 32, 64);

    static const int HIGHLIGHT_PEN_WIDTH = 1;
    static const QColor HIGHLIGHT_PEN_COLOR = QColor(Qt::cyan);
    static const QColor HIGHLIGHT_RECT_COLOR = QColor(0, 255, 255, 128);
}

#endif // SCHEMEKEYS_H
